/* A positive integer is called a palindrome if its representation in the decimal 
system is the same when read from left to right and from right to left. 
For a given positive integer K of not more than 1000000 digits, write the value 
of the smallest palindrome larger than K to output. Numbers are always displayed
without leading zeros. */



function palindrome(num){
    var number =num;
    var i=0;
    while(i===0){
        var sorted = number.toString().split("").reverse().join("");
        if(sorted.toString()===number.toString()){
            return number;
        }
      number++;
    }
}

palindrome(2345678);
2346432
